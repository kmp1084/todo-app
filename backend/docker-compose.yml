services:
  postgres:
    image: postgres:17
    container_name: todos-postgres
    environment:
      POSTGRES_DB: todos
      POSTGRES_USER: todos
      POSTGRES_PASSWORD: todos
    ports:
      - "5432:5432"
    volumes:
      - todos-pg-data:/var/lib/postgresql/data

volumes:
  todos-pg-data: